# Satellite SB

The earlier near-duplicate of ORBITAL, kept as a monument to editing the wrong repo. Its one useful change — scrubbing street-level location references — was merged into orbital-sb; nothing else differs.

## How it works

Same single-file satellite-recon terminal as orbital-sb. The README's first line is "DEPRECATED — use orbital-sb," which is the whole story.

## Stack

Single-file HTML, vanilla JS, Leaflet, globe.gl

## Status

Deprecated. orbital-sb is canonical.
