# AI Cold-Calling Research

A research brief on AI-driven automated cold calling — where LLMs meet voice synthesis, and what it takes to make the round trip fast enough that the person on the other end doesn't hang up.

## How it works

A static HTML brief: implementation strategies for automated outreach, low-latency voice analysis, and a comparison of AI calling architectures. Open `index.html`, read.

## Stack

HTML, CSS.

## Status

Done — it's a document; the implementations it fed live elsewhere (voice-caller, cold-call-bot).
